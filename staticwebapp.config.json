{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "globalHeaders": {
        "cache-control": "max-age=60;"
    },
    "routes": [
        {
            "route": "/",
            "headers": {
                "cache-control": "no-store"
            }
        }
    ]
}