{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "globalHeaders": {
        "cache-control": "max-age=60;"
    },
    "routes": [
        {
            "route": "/*.html",
            "headers": {
                "cache-control": "no-store"
            }
        }
    ]
}